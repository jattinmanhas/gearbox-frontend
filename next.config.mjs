/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'gearboxs3.s3.ap-southeast-2.amazonaws.com'],
    },
};


export default nextConfig;
